name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: 'src-tauri -> target'
          shared-key: 'tauri-build'
          cache-on-failure: true
          save-if: true

      - name: Install dependencies
        run: npm ci

      - name: Debug signing key
        shell: bash
        run: |
          echo "Checking TAURI_SIGNING_PRIVATE_KEY format..."
          KEY_LENGTH=${#TAURI_SIGNING_PRIVATE_KEY}
          echo "Key length: $KEY_LENGTH characters"
          echo "First 20 chars: ${TAURI_SIGNING_PRIVATE_KEY:0:20}"
          echo "Contains newline: $(echo "$TAURI_SIGNING_PRIVATE_KEY" | grep -c $'\n' || echo 0)"
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}

      - name: Build and Release
        uses: tauri-apps/tauri-action@v0.5.16
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ""
        with:
          args: --verbose
          tagName: ${{ github.ref_name }}
          releaseName: 'BallCam Agent ${{ github.ref_name }}'
          releaseBody: |
            Download the installer for your platform:
            - **Windows**: `.msi` or `.exe` (NSIS installer)

            The app will auto-update when new versions are available.
          releaseDraft: false
          prerelease: false
          includeUpdaterJson: true
          updaterJsonPreferNsis: true
